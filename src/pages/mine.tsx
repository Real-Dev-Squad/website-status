import { FC } from 'react';
import Head from '@/components/head';
import Layout from '@/components/Layout';

const Mine: FC = () => (
  <Layout>
    <Head title="Mine" />
  </Layout>
);

export default Mine;
